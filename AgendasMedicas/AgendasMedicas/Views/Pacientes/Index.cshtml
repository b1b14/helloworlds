@model IEnumerable<AgendasMedicas.Models.Paciente>

@{
	ViewData["Title"] = "Cadastro de Pacientes";
}
<div class="container text-light">
	<h1>Cadastro de Pacientes</h1>

	<p>
		<a asp-action="Create" class="btn btn-outline-light btn-lg w-25"><i class="bi bi-plus-circle"></i>Adicionar</a>
	</p>
	<table style="background-color:#56DFCF" class="table table-striped table-borderless">
		<thead>
		<th class="bg-black">
			Prontuário
		</th>
		<th class="bg-black">
				@Html.DisplayNameFor(model => model.Nome)
		</th>
		<th class="bg-black">
				@Html.DisplayNameFor(model => model.CPF)
		</th>
		<th class="bg-black">
			Data Nascimento
		</th>
		<th class="bg-black">
				@Html.DisplayNameFor(model => model.Celular)
		</th>
		<th class="bg-black">
			E-mail
		</th>
		<th class="bg-black">
			Ações
		</th>
		</tr>
		</thead>
		<tbody>
			@foreach (var item in Model)
			{
				<tr>
					<td>
						@Html.DisplayFor(modelItem => item.Prontuario)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.Nome)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.CPF)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.DataNascimento)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.Celular)
					</td>

					<td>
						@Html.DisplayFor(modelItem => item.Email)
					</td>
					<td>
						<a class="btn btn-outline-light btn-sm me-2" asp-action="Edit" asp-route-id="@item.PacienteId"><i class="bi bi-pencil-square"></i> Editar</a>
						<a class="btn btn-outline-light btn-sm" asp-action="Delete" asp-route-id="@item.PacienteId"><i class="bi bi-x-square"></i> Excluir</a>
					</td>
				</tr>
			}
		</tbody>
	</table>
</div>